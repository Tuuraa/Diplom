<UserControl x:Class="WPFComponents.View.FancyBalloon"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFComponents.View"
             mc:Ignorable="d" 
             d:DesignHeight="120" d:DesignWidth="300">
    <UserControl.Resources>
        <!-- Стиль для текста -->
        <Style x:Key="MessageTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="Margin" Value="0,10,0,0"/>
            <Setter Property="Foreground" Value="#DCDCDC"/>
        </Style>

        <!-- Стиль для кнопки с закругленными углами -->
        <Style x:Key="AgreeButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="120"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#FF4A90E2" Offset="0"/>
                        <GradientStop Color="#FF356AC1" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="90,62,0,0"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="True">
                    <DataTrigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <ColorAnimation To="#FF356AC1" Storyboard.TargetProperty="(Button.Background).(GradientBrush.GradientStops)[1].Color" Duration="0:0:0.3"/>
                                <ColorAnimation To="#FF4A90E2" Storyboard.TargetProperty="(Button.Background).(GradientBrush.GradientStops)[0].Color" Duration="0:0:0.3"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </DataTrigger.EnterActions>
                    <DataTrigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <ColorAnimation To="#FF4A90E2" Storyboard.TargetProperty="(Button.Background).(GradientBrush.GradientStops)[1].Color" Duration="0:0:0.3"/>
                                <ColorAnimation To="#FF356AC1" Storyboard.TargetProperty="(Button.Background).(GradientBrush.GradientStops)[0].Color" Duration="0:0:0.3"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </DataTrigger.ExitActions>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <!-- Темный фон с закругленными углами -->
    <Border BorderBrush="Black" BorderThickness="2" Background="{DynamicResource ApplicationBackgroundBrush}" Padding="10" CornerRadius="12">
        <Grid>
            <TextBlock TextTrimming="CharacterEllipsis" x:Name="MessageTextBlock" Style="{StaticResource MessageTextStyle}" Height="51" Margin="0,10,0,0"/>
            <Button Width="50" Height="20"
        x:Name="AgreeButton"
        Content="Ок"
        FontSize="10"
        Style="{StaticResource AgreeButtonStyle}"
        Click="AgreeButton_Click"
        Margin="216,66,10,0">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center"
                                  VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>

        </Grid>
    </Border>
</UserControl>
